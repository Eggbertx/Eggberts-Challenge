//const term = require("term");

const data = require("chipdata");
const game = require("game");
system.abort(Color.of("chartreus"));
data.loadSprites();
data.loadData();
game.start();
system.abort("end!")
